usage: eii [option] [argument] {...}

OPTIONS
  -s     select
  -u     update
  -i     insert
  -d     delete
  -l     list
  -h     help/ Usage

  -t     table
  -c     display column (defaults to filter)

  -f     filter column (overwrite default filter)
  -v     values
         filter follows the following order:
           1) fields supplied in -f arguments
           2) fields supplied in -c arguments
           3) all fields
